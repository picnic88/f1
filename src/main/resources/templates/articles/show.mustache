{{>layouts/header}}

<div class="container p-5">

    {{#article}} <div class="card" style="width: 100%;">
        <div class="card-header">
            게시글 번호: {{id}}
        </div>
        <div class="card-body">
            <h5 class="card-title">{{title}}</h5> <p class="card-text">{{content}}</p> <a href="/articles/new" class="btn btn-primary">새 글 쓰기</a>
            <a href="/articles" class="btn btn-info">목록으로</a>
        </div>
    </div>
    {{/article}} </div>

{{>layouts/footer}}